js/ui.js im showDetails(...)-Block ergänzen/ersetzen
Datei: [js/ui.js](C:\Users\Hagen\Desktop\Buiness\DND\infinite Dungeon\Reposetory\Hagen-Branch\Playable Versions\Version Ceta 1.2 Hagen\js\ui.js:484)
const portraitSrc = c.portrait || '';
const portraitFallback = c.isSummon ? 'SUM' : 'HERO';
const detailPortraitHtml = `
    <div class="hero-detail-header sticky top-0 z-20 rounded-xl overflow-hidden border ${c.isNPC ? (c.isSummon ? 'border-purple-600/50' : 'border-blue-600/50') : 'border-slate-600/60'} shadow-[0_10px_30px_rgba(0,0,0,0.45)] mb-3">
        <div class="hero-detail-header-media ${portraitSrc ? '' : 'hero-detail-header-fallback'}">
            ${portraitSrc ? `<img src="${portraitSrc}" class="hero-detail-header-bg" aria-hidden="true"><img src="${portraitSrc}" class="hero-detail-header-portrait">` : `<div class="hero-detail-fallback-icon">${portraitFallback}</div>`}
            <div class="hero-detail-header-shade"></div>
        </div>
        <div class="hero-detail-header-meta">
            <h3 class="cinzel text-amber-300 text-sm tracking-wide">${c.name} ${sumBadge}</h3>
            <p class="text-[10px] text-slate-200/90">${c.class} • Lvl ${c.level}</p>
            <div class="mt-1.5 w-full bg-black/40 h-1.5 rounded-full border border-white/10 overflow-hidden"><div class="bg-gradient-to-r from-purple-500 to-indigo-400 h-full" style="width: ${(c.xp / (c.level * 100)) * 100}%"></div></div>
            <p class="text-[9px] text-slate-300/80 mt-1">${c.xp}/${c.level * 100} XP</p>
        </div>
    </div>
    <div class="hero-details-stack">
`;
Und beim Rendern:

DOM.detailsContent.innerHTML = `
    ${detailPortraitHtml}
    ...
`;
Sicherstellen, dass am Ende der Details ein zusätzliches </div> für hero-details-stack geschlossen wird.

CSS-Block für Hero-Profilbild-Header einfügen/ersetzen
Datei: [style.css](C:\Users\Hagen\Desktop\Buiness\DND\infinite Dungeon\Reposetory\Hagen-Branch\Playable Versions\Version Ceta 1.2 Hagen\style.css:442)
/* Hero Detail Sticky Header */
#char-details {
    scroll-padding-top: 0.5rem;
}

#details-content .hero-detail-header {
    position: sticky;
    top: 0;
}

.hero-detail-header-media {
    position: relative;
    height: auto;
    aspect-ratio: 3 / 4;
    min-height: 12rem;
    background: radial-gradient(circle at 50% 15%, rgba(59,130,246,0.22) 0%, rgba(15,23,42,0.86) 70%);
}

.hero-detail-header-bg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: contain;
    filter: blur(8px) brightness(0.55) saturate(0.9);
    transform: scale(1.06);
}

.hero-detail-header-portrait {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center center;
    padding: 0;
    filter: drop-shadow(0 8px 16px rgba(0,0,0,0.45));
    background: transparent;
}

.hero-detail-header-fallback {
    display: flex;
    align-items: center;
    justify-content: center;
}

.hero-detail-fallback-icon {
    font-size: 3rem;
    opacity: 0.8;
    filter: drop-shadow(0 0 10px rgba(255,255,255,0.2));
}

.hero-detail-header-shade {
    position: absolute;
    inset: 0;
    background: linear-gradient(to bottom, rgba(2,6,23,0.00) 0%, rgba(2,6,23,0.16) 38%, rgba(2,6,23,0.42) 68%, rgba(2,6,23,0.60) 100%);
}

.hero-detail-header-meta {
    position: absolute;
    left: 0;
    right: 0;
    bottom: 0;
    padding: 0.65rem 0.8rem;
    backdrop-filter: blur(6px);
    -webkit-backdrop-filter: blur(6px);
    background: linear-gradient(to top, rgba(15,23,42,0.65), rgba(15,23,42,0.3));
}

#details-content .hero-details-stack {
    position: relative;
    z-index: 30;
    margin-top: -1.1rem;
    padding-top: 1.1rem;
}

#details-content .hero-details-stack::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1.8rem;
    background: linear-gradient(to bottom, rgba(2,6,23,0.48) 0%, rgba(2,6,23,0.24) 45%, rgba(2,6,23,0.00) 100%);
    pointer-events: none;
}

.hero-details-stack > div {
    background: rgba(15, 23, 42, 0.68);
    border: 1px solid rgba(148, 163, 184, 0.22);
    border-radius: 0.75rem;
    padding: 0.55rem;
    margin-bottom: 0.6rem;
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
}

.hero-details-stack > div h4 {
    color: #cbd5e1;
}